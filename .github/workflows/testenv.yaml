name: testing-env
on:
  push:
    branches:
    - develop
    - main
    
env: 
  VEG: ${{ vars.VAR1 }} 
  
jobs:
  PrintEnv:
    name: Print Env according to branch
    runs-on: ubuntu-latest
    environment: ENV2
    steps:
    - run: echo ${{ env.VEG }} hello
    
      
